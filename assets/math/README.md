# Rendered mathematical expressions

The teaching guides, model derivation and iteration protocol embed these self-contained vector equations using ordinary Markdown image links. No LaTeX extension, remote image host or HTML document is required. Inline expressions have a smaller intrinsic size than display equations. The original LaTeX for each expression is preserved inside its SVG metadata as `latex-source`. Keep this directory with the Markdown files when moving the project.
